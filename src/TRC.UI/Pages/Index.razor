@page "/"

<h1>TRC Platform</h1>

<p>
  This UI is intentionally thin. The canonical logic lives in <code>TRC.Domain</code>.
  Start by running backtests via <code>TRC.Tools</code>, then wire this UI to <code>TRC.Api</code>.
</p>

<p>
  Next up: backtest runner view, results explorer, equity curve, event timeline.
</p>
